<table class="tab0">
  <tr>
    <td>
      <a href="#" title="Home">¦</a> »
      <%= link_to "Imperialní kartografie", systems_path %> »
      <a href="#">Událost</a>
    </td>
  </tr>
</table>

<table class="tab1">
    <tr>
        <td style="padding: 10px; width:270px; height: 200px; background-image: url(<%= asset_path 'imghra/kolonizace.png' %>) !important">

          <div>
            <li><h2><%= @udalost.name %></h2></li>
            <li><%= @udalost.description %></li>
              <br>
              <br>
              <br>
              <br>
            <li><b>Trvání:</b> <%= @leno.duration + 1 if @leno %> <%= @planet.duration + 1 if @planet %>
        <% id = @leno if @leno %>
        <% id = @planet if @planet %>
            <li> Je možné investovat finance do jejího øešení a událost tak eliminovat.</li>
              <%= form_tag oprava_katastrofy_path(:id => id, :typ => @leno ? "L" : "P"), :class => "inline_list" do %>
              <li><%= button_to @leno.price.to_s + " S", :class => "ok", oprava_katastrofy_path(:id => @leno.id, :typ => "L") if @leno %></li>
              <li><%= button_to @planet.price.to_s + " S", :class => "ok", oprava_katastrofy_path(:id => @planet.id, :typ => "P") if @planet && current_user.leader? %></li>
            </ul>
            
        <% end %>
        
        </td>
    </tr>
</table>